<section>
  <div>
    <h1><%= @user.username %>"'s profile page"<h1>
    <ul>
      <% @surveys.each do |survey| %>

        <li><a href="#"><%= survey.title %></a></li>
      <% end %>
    </ul>
  </div>
